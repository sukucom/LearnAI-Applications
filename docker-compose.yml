version: '3.8'

services:
  ai:
    build: .
    container_name: ai_project
    volumes:
      - .:/app
    ports:
      - "8888:8888"
      - "7860:7860"  # Gradio chatbot
    command: >
      jupyter notebook --ip=0.0.0.0 --port=8888 --allow-root --no-browser
